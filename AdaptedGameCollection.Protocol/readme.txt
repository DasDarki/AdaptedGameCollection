The Protocol defines all packets which are sent back end forth.

Packets which are sent FROM the client TO the server are in the "Client" folder.
Packets which are sent FROM the server TO the client are in the "Server" folder.